import React from "react";
import ReactDOM from "react-dom/client";

import BrotApp from "./components/BrotApp";

const domContainer = document.getElementById("root");
const root = ReactDOM.createRoot(domContainer);
root.render(<BrotApp />);